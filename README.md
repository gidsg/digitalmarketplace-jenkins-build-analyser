# Jenkins build analyser

## Setup

`python -m venv ./venv`

`source venv/bin/activate`

`pip install -r requirements.txt`

Populate `.env.example` and rename to `.env`

## Running app

`python app.py`

